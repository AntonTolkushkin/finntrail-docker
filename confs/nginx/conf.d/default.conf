# Без environment-specific override Nginx остаётся доступен только на порту,
# заданном в базовом Compose. Local и production подменяют этот файл.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    include /etc/nginx/inc/bitrix-server.conf;
}

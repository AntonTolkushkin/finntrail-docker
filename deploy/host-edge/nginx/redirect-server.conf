# Include this file inside the HTTP and HTTPS server blocks for finntrail.ru.
# Using "if" with only "return" is safe in the Nginx rewrite module.

if ($finntrail_redirect_301) {
    return 301 $finntrail_redirect_301;
}

if ($finntrail_redirect_302) {
    return 302 $finntrail_redirect_302;
}
